framework:
    rate_limiter:
        register:
            policy: fixed_window
            limit: 10
            interval: '1 hour'
            cache_pool: cache.rate_limiter

        api:
            policy: token_bucket
            limit: 60
            rate: { amount: 60, interval: '1 minute' }
            cache_pool: cache.rate_limiter
